{{ config(
    cluster_by = "_airbyte_emitted_at",
    partition_by = {"field": "_airbyte_emitted_at", "data_type": "timestamp", "granularity": "day"},
    unique_key = '_airbyte_ab_id',
    schema = "test_normalization",
    tags = [ "top-level" ]
) }}
-- Final base SQL model
-- depends_on: {{ ref('pull_request_stats_ab3') }}
select
    id,
    url,
    base,
    body,
    head,
    user,
    draft,
    state,
    title,
    _links,
    labels,
    locked,
    merged,
    number,
    commits,
    node_id,
    assignee,
    comments,
    diff_url,
    html_url,
    additions,
    assignees,
    closed_at,
    deletions,
    issue_url,
    mergeable,
    merged_at,
    merged_by,
    milestone,
    patch_url,
    auto_merge,
    created_at,
    rebaseable,
    updated_at,
    commits_url,
    comments_url,
    statuses_url,
    changed_files,
    mergeable_state,
    requested_teams,
    review_comments,
    merge_commit_sha,
    active_lock_reason,
    author_association,
    review_comment_url,
    requested_reviewers,
    review_comments_url,
    maintainer_can_modify,
    _airbyte_ab_id,
    _airbyte_emitted_at,
    {{ current_timestamp() }} as _airbyte_normalized_at,
    _airbyte_pull_request_stats_hashid
from {{ ref('pull_request_stats_ab3') }}
-- pull_request_stats from {{ source('test_normalization', '_airbyte_raw_pull_request_stats') }}
where 1 = 1

